#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void double_point_execute_action(char** actions, FILE* file, char* str, size_t size){
	char* first_command = (char*) malloc(7 * sizeof(char));
	strcpy(action, first_command);

	if(strcmp(first_command, "create")){
			
	};

}
